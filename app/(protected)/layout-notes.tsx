export default function Notes() {
  return null
}

/*
This placeholder exists to ensure the (protected) segment is recognized.
Actual layout code is in app/(protected)/layout.tsx (client component).
*/
